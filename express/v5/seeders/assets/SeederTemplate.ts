export const runSeeder = async (): Promise<void> => {
    // 1) connect
    // 2) define stable seed data
    // 3) find by unique key
    // 4) update if drifted else create
    // 5) log created/updated/skipped summary
};
